When pushing to heroku how do I correctly reference
DataMapper.setup(:default, "postgres://localhost/chittter_#{env}")
in data_mapper_set_up?

/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\/\
spec refactoring

def add_peep content = 'test'
  post = Peep.create(content: content, time: Time.now)
end

replying_to_a_peep * extracting breaks this

def sign_in(email = 'test@test.com', password='12345')
  visit '/sessions/new'
  fill_in :email, with: email
  fill_in :password, with: password
  click_button 'log in'
end

replying_to_a_peep

