CREATE TABLE peeps(id SERIAL PRIMARY KEY, date TIME, peep VARCHAR(140));

-- Chitter=# CREATE TABLE Chitter (
-- Chitter(# id SERIAL PRIMARY KEY,
-- Chitter(# date TIME,
-- Chitter(# peep VARCHAR(140));
