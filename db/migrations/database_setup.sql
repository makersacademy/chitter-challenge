CREATE DATABASE chitter;
CREATE TABLE peeps (id SERIAL PRIMARY KEY, text varchar(280), date date(dd-mm-yyyy));
CREATE DATABASE chitter_test;
CREATE TABLE peeps (id SERIAL PRIMARY KEY, text varchar(280)); date date(dd-mm-yyyy);