CREATE DATABASE users
CREATE TABLE user(id SERIAL PRIMARY KEY, email VARCHAR(40), password VARCHAR(25), name VARCHAR(15), username VARCHAR(20));

CREATE DATABASE peeps
CREATE TABLE peeps(id SERIAL PRIMARY KEY, peep VARCHAR(100), date DATE, time TIMESTAMP, user_id INTEGER REFERENCES users (id));
