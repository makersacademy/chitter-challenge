CREATE TABLE users (id SERIAL PRIMARY KEY,
                                      email VARCHAR(60),
                                            password VARCHAR(140),
                                                     display VARCHAR(40),
                                                             handle VARCHAR(20));