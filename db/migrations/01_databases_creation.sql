CREATE TABLE users( id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, username VARCHAR(30) UNIQUE NOT NULL, email VARCHAR(255) UNIQUE NOT NULL, password VARCHAR(255) NOT NULL);
CREATE TABLE peeps( id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id) NOT NULL, content VARCHAR(280) NOT NULL, created_on TIMESTAMP NOT NULL);