services:
  - postgresql

postgres:
  adapter: postgresql
  database: chitter_app_test
  username: postgres

before_script:
  - psql -U postgres -c 'CREATE DATABASE chitter_app_test;'
  - 'rake db:migrate'

script:
  - bundle exec rspec

