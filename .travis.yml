language: ruby

rvm: '2.2.3'

script:
  - mkdir -p spec && bundle exec rspec spec

before_install:
- sed -i '2d' Gemfile
- sudo apt-get update -qq
- sudo apt-get install -qq postgresql-server-dev-9.4.5

before_script:
  - psql -c 'create database chitter_test;' -U postgres
