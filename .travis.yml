language: ruby

services:
  - postgresql

before_script:
  # - psql -U postgres -c 'CREATE DATABASE chitter_test;'
  - psql < db/migrate/01-create_databases.sql
  - rake db:migrate

script:
  - bundle exec rspec
