services:
  - postgresql
  - activerecord

before_script:
  - psql -U postgres -c 'CREATE DATABASE chitter_test;'
  - psql -U postgres -c user_test "CREATE table users (id SERIAL PRIMARY KEY, email VARC
HAR(60), password VARCHAR(140), name VARCHAR(250);"
  - psql -U postgres -c messages_test "CREATE table messages (id SERIAL PRIMARY KEY, content
 VARCHAR(300), time_created TIMESTAMP, user_id INTEGER REFERENC
ES users_test (id));"

script:
  - bundle exec rspec

language: ruby
