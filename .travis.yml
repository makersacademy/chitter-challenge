rvm: '2.5.0'

script:
    bundle exec rake setup
  - mkdir -p spec && bundle exec rspec 

before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq postgresql-server-dev-9.3

before_script:
  - psql -c 'create database travis;' -U postgres
