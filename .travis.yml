rvm: '2.2.1'

before_script:
  - psql -c 'create database fridgedoor_test;' -U postgres

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq postgresql-server-dev-9.3

script: 
  - bundle exec rspec spec
  - bundle exec cucumber