rvm: '2.4.0'

script:
 - bundle exec rake db:auto_migrate_test
 - mkdir -p spec && bundle exec rspec spec


before_install:
- sed -i '2d' Gemfile
- sudo apt-get update -qq
- sudo apt-get install -qq postgresql-server-dev-9.3

before_script:
 - psql -c 'create database chitter_app_test;' -U postgres
