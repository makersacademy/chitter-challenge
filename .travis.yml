language: ruby

services:
  - postgresql

before_script:
  - psql -U postgres -c 'CREATE DATABASE chitter_test;'
  # - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - bundle exec rspec
