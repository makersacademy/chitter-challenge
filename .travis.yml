services:
  - postgresql

before_script:
  - psql -c 'create database chitter-challenge-test;' -U postgres
  - psql -d chitter-challenge-test -f db/migrations/01_create_peeps_table.sql -U postgres
  - psql -d chitter-challenge-test -f db/migrations/02_add_posted_at.sql -U postgres

script:
  - bundle exec rspec
