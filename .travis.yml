services:
  - postgresql

before_script:
  - psql -U postgres -c 'CREATE DATABASE chitter_test;'
  - 'CREATE TABLE peeps(id SERIAL PRIMARY KEY, message VARCHAR(280), time VARCHAR(60));'


script:
  - bundle exec rspec
