{
  "language": "ruby",
  "os": [
    "linux"
  ],
  "dist": "xenial",
  "services": [
    "postgresql"
  ],
  "before_script": [
    "psql -U postgres -c 'CREATE DATABASE chitter_test;'"
    "psql -U postgres -c 'CREATE TABLE peeps(id SERIAL PRIMARY KEY, body VARCHAR(140), date VARCHAR, username VARCHAR);'"
    "psql -U postgres -c'CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(50), username VARCHAR(20), email VARCHAR(50), password VARCHAR(20));'"
  ],
  "script": [
    "bundle exec rspec"
  ]
}
