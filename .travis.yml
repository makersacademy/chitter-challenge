language: ruby

services:
  - postgresql

before_script:
  - psql -U postgres -c 'create database chitterverse_test;'
  - psql -U postgres -d 'chitterverse_test' -f ./db/migrations/.*sql

script:
  - bundle exec rspec
