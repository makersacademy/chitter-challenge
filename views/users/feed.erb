<!DOCTYPE html>
<html>
  <head>
  <style>
     .content {
      max-width: 500px;
      margin: auto;
  }
      * {
        font-family: sans-serif;
        color: green;
        font-size:x-large;
      }

      button {
    background-color: green;
    color: white;
  }
  table, th, td {
    border: 1px;
}
      </style>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="content">
    <form action="/sessions/destroy" method="post">
      <button type="submit" value="sign out">Sign Out</button>
    </form>
    <h1>Welcome <%= @user.username %>!</h1>
    <form action="/users/<%= @user.id %>/comments" method="post">
     <textarea name="text">
     </textarea>
     <button type="submit" value="peep">Peep</button>
     <table style="width:90%" border="2">
       <tr>
         <th><h2>Chitter Feed<h2><th>
      </tr>
        <% @comments.each do |comment| %>
        <tr>
          <td>
          <%= @user.name %> (<%= @user.username %>) says...<br />
          <strong><%= comment.text %></strong><br />
          <i><%= comment.time %><i><br />
        </td>
        </tr>
        <% end %>
     </table>
     <%= @comment_composer %>
 </form>
</div>
  </body>
</html>
