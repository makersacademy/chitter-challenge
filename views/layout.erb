<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <body>
    <header>
      <h1>Chitter</h1>

      <form action="/users/new">
        <button type="submit" name="sign-up"<%= @user.signup %>>Sign Up</button>
      </form>

      <form action=<%= @user.log_button[:path_action] %>>
        <button type="submit" name=<%= @user.log_button[:name] %>><%= @user.log_button[:label] %></button>
      </form>

    </header>
    <%= yield %>
  </body>
</html>
