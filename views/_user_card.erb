<div class="bg-light rounded mt-3 p-4 pt-4">
  <div class="d-flex flex-row justify-content-between w-100">
    <img src="<%= image_picker(@user.id)%>" alt="twbs" width="50" height="50" class="rounded-circle flex-shrink-0">
    <div class="d-flex flex-column mx-3 w-100 justify-content-center">
      <h6 class="mb-0 "><%= @user.name %></h6>
      <div class="m-1"></div>
      <small class="mb-0 text-muted">@<%= @user.username %></small>
    </div>
    <form action="/sessions/destroy" method="post">
      <input class="m-2 btn    btn-primary" type="submit" style="font-family: 'Raleway', sans-serif;"
             id="Sign Out" value="Sign Out" />
    </form>
  </div>
</a>
</div>
