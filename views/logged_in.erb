<html>
  <head></head>
  <body>
  <form action="/" method="get">
  <a href='/'>Click here to sign out</a>
  </form>
    <h1>Peeps</h1>
    <% @peep_repo.all.reverse.each do |peep| %>
      <div>
      <p><%= @user_repo.find(peep.user_id).username %>: <%= peep.content %></p> 
      <a href="/comment/<%= peep.id %>">Comment</a>
      <p><%= peep.time_posted %>
    </div>
    <% end %>
  </body>
</html>