<!DOCTYPE html>
<html>
<head>
  <title>Peeps</title>
</head>
<body>
  <h1>Peeps</h1>
  <ul> <% @peeps.each do |peep| %>
    <li> <%= peep.peep %> <%= peep.time %>
         <%= peep.user['name'] %>
         <%= peep.user['username'] %>
    </li>
    <% end %>
  </ul>
</body>
</html>
