<ul>
  <% @peeps.each do |peep| %>
    <li class="peep" id="peep-<%=peep.id %>">
      <%= peep.peep %> - <%= peep.created_at %></li>
      <input type="submit" value="Delete" />
    </li>
  <% end %>
</ul>
