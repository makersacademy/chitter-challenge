<!DOCTYPE html>
<html>
<head>

</head>
<body>
  <% if @current_user %>
    <div>
      <form action='/sessions' method='post'>
        <input type='hidden' name='_method' value='DELETE'/>
        <input type='submit' value='Log out'/>
      </form>
    </div>
    <div>
      <p><%= @current_user.name %></p>
      <p>@<%= @current_user.username %></p>
    </div>
  <% else %>
    <div>
      <a href='/'>Log in</a>
    </div>
  <% end %>

  <% @peeps.each do |peep| %>
    <div>
      <p><%= @user_class.select_by_id(peep.user_id).name %></p>
      <p>@<%= @user_class.select_by_id(peep.user_id).username %></p>
      <p><%= peep.text %></p>
      <p><%= peep.time %></p>
    </div>
  <% end %>

  <% if @current_user %>
    <a href='/users/<%= @current_user.id %>/peeps/new'>New peep</a>
  <% end %>
</body>
</html>
