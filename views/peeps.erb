<html>
  <head></head>
    <body>

      <h1> Peeps </h1>
      <% @peeps.each do |peep| %>
      <div>
      <% user = @users.find{|user| user.id == peep.user_id}%>
      <b><%= user.name%> @<%=user.username%> 
      <br>
      <%= peep.message%> <%=peep.time_stamp%>
      </div>
      <br>
      <br>
      <%end%>
    </body>
</html>
