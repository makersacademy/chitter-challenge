<h2> All peeps!</h2>
<% @all_peeps.each do |peep| %>
<p>
  <form action = '/chitter/peep/<%=peep.id%>' method = "get">
  <%= peep.message %> <button type='submit' >Detail</button>
</p>
<% end%>
<p>
  <form action = '/chitter/maker/homepage' method = "get">
    <button type='submit' >back to homepage</button>
</p>
