<html>
  <head>
    <title>Chitter</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1> Welcome to Chitter, <%=@name%>! </h1>    
    <h2> What's peepin': </h2>
      <% if @peeps_reverse_chron_order.empty? %>
        <p> 'No one's made a peep!' </p>
      <% else %>
        <% @peeps_reverse_chron_order.each do |peep| %>
          <p> <%= peep.text %> - <%= peep.user.name %> - peeped at: <%= peep.created_at %> </p>
        <% end %>
      <% end %>
    <div>
    <a href="/user/compose_peep"> Compose Peep </a> <br>
    <a href="/logout"> Log Out </a>
    </div>
  </body>
</html>