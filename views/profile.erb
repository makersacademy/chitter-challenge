<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="<%= url("/css/style.css")%>">
    <title>Chitter</title>
  </head>

  <body>
    <div class="container">

    <div class="signout">
      <form action="/sessions" method="post">
        <input type="hidden" name="_method" value="delete">
        <input type="submit" name="Sign Out" value="Sign Out">
      </form>
    </div>
      
    <header>
    <h2> Welcome to Chitter <%=@user.name%> </h2>
    </header>

    <form action=<%="/peep/#{@user.id}"%> method="post">
      <div class="post_peep">
        What's on your mind? <input type="text" name="content" placeholder="Send a peep!" required>
        <input type="submit" name="Post" value="Post">
        <br>
        <br>
        <p> The latest peeps:
      </div>
    </form>

    <% @peeps.reverse.each do |peep| %>
    <br>
    <div class=display_peeps>
      <p> <%=peep.user.name%> (AKA: <%=peep.user.username%>) peeped: </p> 
      <p><%=peep.content%> </p>
      <p>Posted:<%=peep.created_at.strftime('%d-%m-%Y | %H:%M')%>
    </div>
    <% end %>
    </div>
</body>
</html>
