<!DOCTYPE html>
<html>
  <head>
    <style>
      #my_table {
        font-family: sans-serif;
        font-size: 18px;
        text-align: center;
        border-collapse: collapse;
        width: 80%;
      }

      #my_table td, #my_table th {
        border: 1px solid #ddd;
        padding: 5px;
      }

      #my_table th {
        padding-top: 7px;
        padding-bottom: 7px;
        background-color: #8A8A8A;
        color: white;
    </style>
  </head>

  <body>

    <table id="my_table">
      <tr>
        <% @database[0].keys.each do |item| %>
        <th><%= item %></th>
        <% end %>
      </tr>
        <% @database.each do |item| %>
          <tr>
            <% item.each do |key_value| %>
              <td><%= key_value[1] %></td>
            <% end %>
          </tr>
        <% end %>
      <tr>
    </table>

    <p></p>

  <table id="my_table">
    <tr>
      <% @dataline[0].keys.each do |item| %>
      <th><%= item %></th>
      <% end %>
    </tr>
      <% @dataline.each do |item| %>
        <tr>
          <% item.each do |key_value| %>
            <td><%= key_value[1] %></td>
          <% end %>
        </tr>
      <% end %>
    <tr>
  </table>



  </body>
</html>
