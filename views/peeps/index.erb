<h1> Posted Peeps </h1>
<% if @user %>
  <h2> Welcome, <%= @user.username %></h2>
<% end %>
<ul>
  <% @peeps.reverse.each do |peep| %>
  <li class='peep'><%= peep.text %>
    <%= peep.timestamp[0, 16]%>
    <%= User.find(peep.user_id).username%>
  </li>
  <% end %>
</ul>