<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Home / Chitter</title>
  </head>
  <body>
    <h2>Chitter</h2>
    <% if @user %>
      <h4>Welcome, <%= @user.username %></h4>
      <a href="/peeps/new">Create new peep!</a><br>
    <% end %>
    <ul>
      <% @peeps.each do |peep| %>
        <li><%= peep.message %>  -  <%= DateTime.parse(peep.time).strftime('%e-%m-%y %H:%M') %></li>
      <% end %>
    </ul>


    <br><a href="/users/new">Sign up</a> | <a href="/sessions/new">Sign in</a>

  </body>
</html>
