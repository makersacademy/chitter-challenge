<% if @profile %>
  <h1>Welcome, <%= @profile.name %></h1>
<% end %>
<ul>
  <% @peeps.reverse_each do |peep| %>
    <li class="peep" id="peep-<%= peep.id %>">
      <%= peep.peep %> PEEPED BY:<%= @profile.username %>( <%= @profile.name %> ) AT: <%= peep.peeped_at %>
    </li>
  <% end %>
</ul>
