<!DOCTYPE html>
<html>
  <head>
    <title>Peeps</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <h1>Peeps</h1>
      <% if @user %>
        <h2>Welcome, <%= @user.name %></h2>
      <% end %>
    <h2>Here are your peeps:</h2>

    <% @peeps.each do |peep| %>
      <div class='peep'>
        <%= peep.content %>
        posted by <%= peep.name %> (<%= peep.username %>)
        on <%= peep.time %>
      </div></br>
    <% end %>

    </br>
    <a href="/peeps/new"><button>Create new peep</button></a>
    </br>
    <a href="/"><button>Homepage</button></a>
  </body>
</html>
