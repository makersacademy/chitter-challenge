<% if @user %>
  <h2>Welcome, <%= @user.email %></h2>
<% end %>

<ul class="list-group">
    <% @peeps.reverse_each do |peep| %>
      <ol class="list-group-item">
        <b><%= peep.message %></b>
        <br>
        Posted at:
        <%= peep.created_at %>
      </ol>
    <% end %>
