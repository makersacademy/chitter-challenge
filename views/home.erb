<h1> Username: </h1>

<ul>
  <% @peep.each do |peep| %>
    <h3>
    <%= peep.body %>
    </h3>
    <div></div>
  <% end %>
</ul>


<form action='/addpeep'>
<button action='/addpeep' name="Add Peep" >Add Peep </button>
</form>