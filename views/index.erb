<h1>Welcome to Chitter</h1>
<a href='/sign_up'>Sign up</a>
<a href='/new_peep'>New peep</a>

<h2>All peeps</h2>
<ul>
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.content %> (<%= peep.created_at.strftime('%H:%M %e %b %Y') %>)
    </li>
  <% end %>
</ul>

