<!DOCTYPE html>
<html lang="en">
<head>
  <title>Chitter</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="" />
  <link rel="stylesheet" type="text/css" href="styles.css" />
  <link rel="icon" href="img/favicon-32x32.png">
</head>
<body>
  <header>
    <% if @user %>
      <%= @user.name %>
    <% end %>

    <% if @user %>
      <a class="button" href="/user/signout">Sign Out</a>
    <% else %>
      <a class="button" href="/user/signup">Sign Up</a>
      <a class="button" href="/user/signin">Sign In</a>
    <% end %>
  </header>
  <h1>Chitter</h1>

  <% if @user %>
    <section id="new-peep">
      <h2>Create Peep</h2>
      <form action='/add_peep' method="POST">
        <label for="peep">Peep: </label>
        <input type="text" maxlength="280" name="peep">
        <input type="hidden" value="1" name="user">
        <input type="submit" value="Submit">
      </form>
    </section>
  <% else %>
    <section id="sign-in-promt">
      <h2>Sign in to start peeping</h2>
    </section>
  <% end %>

  <section id="timeline">
    <h2>Peep Timeline</h2>
    <ul>
      <% @peeps.each do |peep| %>
        <li class="timeline">
          <div class="timeline_peep"><%= peep.peep %></div>
          <div class="timeline_user"><%= peep.user %></div>
          <div id=<%"#{peep.peep_id}"%> class="timeline_timestamp"><%= peep.timestamp %></div>
        </li>
      <% end %>
    </ul>
  </section>
</body>
</html>
