<h1>Welcome to Chitter </h1>

<form action='/add_peep'>
  <button type='submit'>Add Peep</button>
  </form>

<ul>
  <% @timeline.each do |peep| %>
    <li><%= peep.created_at %>
      <%= peep.peep %>
    </li>
  <% end %>
</ul>
