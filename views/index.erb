<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Chitter</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="" rel="stylesheet">
  </head>
  <body>
    <% if @user_session %>
      <form method="POST" action="/logout">
       <input type="submit" value="Logout">
      </form>
  <% else %>
    <a href="/sign-up">SIGN UP</a>
    <a href="/login">LOG IN</a>
    <% end %>
    <% if @user_session %>
    <div>
      <form method='POST' action='/new-post'>
          <div>
            <textarea name="content" rows="4" cols="50" placeholder="<%= @user_session_name %>, What's happening?"></textarea>        
          </div>          
          <div>
            <input type="submit">
          </div>
      </form>
    </div>
    <% end %>
    <% @posts_list.each do |post| %>
        <div>
          <p><span><%= post[:name] %></span><span>@<%= post[:username] %></span><span> - <%= post[:time_diff] %></span></p>
          <p><%= post[:content] %></p>
        </div>
        <% end %>
  </body>
</html>
