
<html>
<head>

<style media="screen">

    body {
      background-color: #ABACAB;
      font: 200 20px/1.5 Helvetica, Verdana, sans-serif;
    }

    .new_cheet {
      margin: auto;
    }

    input[type=submit] {
      cursor: pointer;
      background-color: #666;
      color: #fff;
      border-radius: 10px;
      padding: .8em 2em;
    }

    input[name=cheet] {
      width: 80%;
      margin-left: 5%;
    }

    input[type=text] {
      border: 1px solid #e6e9ec;
      border-radius: 8px;
      box-shadow: inset 0px 1px 3px 0px rgba(0,1,1,0.06);
      height: 44px;
      padding: 0 1em;
    }

    .cheet_block {
      list-style-type: none;
      padding-left: 0.1em;
      padding-right: 0.2em;
      padding-bottom: 0.2em;
      background-color: #d1e5e1;
      border-radius: 10px;
    }

    ul {
      list-style-type: none;
      background-color: #d1e5e1
      padding: 0;
      margin: 0;
    }

    .messages {
      width: 60%;
      margin: auto;
      font-size: 1.5em;
    }

    sub {
      font-size: 0.6em;
      vertical-align: top;
    }

</style>
</head>

<h1 style="text-align: center;"> Chitterfeed </h1>

<div class=messages>

  <div class=new_cheet >
    <form action="/new-cheet" method="post">
      <input type="text" name="cheet" placeholder="Cheet">
      <input type="submit" value="Submit" >
    </form>
  </div>

  <ul class=cheet_block>
    <% @cheets.each do |cheet| %>
    <li>
      <ul>
        <li> <%= cheet.cheet %> </li>
        <li><sub> <%= cheet.user %> @ <%= cheet.timestamp[0...-7] %></sub></li>
      </ul>
    </li>
    <% end %>
  <ul>
</div>
