<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <body>
    <h1>Chitter!</h1>
    <% if @user %>
      <h2>Welcome <%= @user.username %>, take a look the chitter peeps!</h2>
      <form action="/peeps/new" method="get">
        <input type="submit" value="Add Peep">
      </form>
      <h3>Peepline:</h3>
      <% @peeps.each do |peep| %>
        <li class="peep"> <%= peep.text %> posted by <%= @users.find(id: peep.user_id).username %> at <%= peep.tstz %></li>
      <% end %>
    <% end %>
    <% unless @user %>
    Already a Chitter User?
    <form action="/sessions/new" method="get">
      <input type="submit" value="Log In">
    </form>
    <br>
    Sign Up to use Chitter
    <form action="/users/new" method="get">
      <input type="submit" value="Sign Up">
    </form>
    <% end %>
  </body>
</html>
