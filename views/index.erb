<div>
  <% if @logged_in_user %>
    <h2>Welcome <%= @logged_in_user.name %>!</h2>
  <% else %>
    <a href="/sign-up">sign up</a>
  <% end %>
</div>
<div>
  <% @peeps.each do |peep| %>
    <h3><%= peep.user.name %> - <%= peep.user.username %></h3>
    <p><%= peep.content %></p>
  <% end %>
</div>