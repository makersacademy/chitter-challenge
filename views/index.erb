<html>
  <head>
    <title>Chitter</title>
  </head>
  <body>
    <h1> Welcome to Chitter! </h1>
    <div>
      <a href="/signup"> Sign up </a>
    </div>
    <div>
      <a href="/login"> Log in </a>
    </div>
    <h2> What's peepin': </h2>
    <% if @peeps_reverse_chron_order.empty? %>
      <p> 'No one's made a peep!' </p>
    <% else %>
      <% @peeps_reverse_chron_order.each do |peep| %>
        <p> <%= peep.text %> - peeped at: <%= peep.created_at %> </p>
      <% end %>
    <% end %>
  </body>
</html>