<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <body>
    <header>
      <h1>Chitter</h1>

      <p>Welcome, <%= @user.email %></p>

      <form action="/users/new">
        <button type="submit" name="sign-up">Sign Up</button>
      </form>
    </header>

    <form class="new_peep" action="/chitter" method="post">
      <input type="text" name="peep" placeholder="type your peep here">
      <button type="submit" name="submit">Send Peep</button>
    </form>

    <% @peeps.each do |peep| %>
    <article>
      <address rel="author"><%= peep.user_email %></address>
      <time><%= peep.timestamp %></time>
      <p><%= peep.peep %></p>
    </article>
    <% end %>
  </body>
</html>
