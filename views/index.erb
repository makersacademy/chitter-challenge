<!DOCTYPE html>
<html>

  <head>
    <link rel="stylesheet" href="/css/style.css"/>
    <link rel="icon" type="image/png" href="/images/favicon-32x32.png" sizes="32x32"/>
    <link rel="icon" type="image/png" href="/images/favicon-16x16.png" sizes="16x16"/>
  </head>

  <body>
    <h1>Welcome to Chitter</h1>

    <% if @user %>
      <h1>Welcome, <%= @user.username %></h1>
      <p>

        <a href="/user/logout">
          <button class="postpeep" type="button" value="Logout">
            <span class="icon"></span>Logout</button>
        </p>
      </p>
      <%else%>
      <%if @registered%><p>
        Thank you for registering</p>
        <p>
          <form action="/user/login" method="post">

          <p>
          <input type="text" name="username" placeholder="enter your desired username" /></p>
          <br />
          <input type="text" name="password" placeholder="enter your password" /></p>
          </p>

          <p>

            <button type="submit" class="peep_only_btn" value="Login"><span class="icon"></span>Login</button>
          </p>
        </form></p>
        <%else%>
      <p>You need to register: </p>
        <a href="/user/register">
          <button class="postpeep" type="button" value="Register for Chitter">
            <span class="icon"></span>Register for Chitter</button>
        </p>
        <%end%>
    <% end %>


      <!-- <button class="button"><span class="icon"></span>Button</button> -->
      <p>
        <a href="/peep">
          <button class="postpeep" type="button" value="Post a Peep">
            <span class="icon"></span>Post a Peep</button>
        </a>
      </p>

      <%
    @peeps.each do |peep_row|%>
        <!-- <%=peep_row%> -->
      </p>
      <p>Peep:
        <%=peep_row[:peep]%>
        - sent
        <%=peep_row[:posted_date]%></p>
    <%end%>
  </body>
</html>
