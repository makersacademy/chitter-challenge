<h1>Chitter</h1>
<p>In order to add a peep (message), you need to <strong><a href="/register/new">Sign-up</a></strong>.</p>
<h2>Latest peeps</h2>
<ul>
  <% @messages.each do |message| %>
    <li>
      <h2>
        <%= message.message %>
      </h2>
      <p>Date and time of peep: <%= message.date %></p>
      <% message.users.each do |user| %>
        <p>Peep from: <%= user.first_name %>&nbsp;<%= user.last_name %></p>
      <% end %>
    <ul>
    </ul>
  <% end %>
</ul>
