<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <link rel="stylesheet" type="text/css" href="<%= url("/style/style.css") %>">
  <meta charset="utf-8">
  <title>Chitter: Sign In</title>
</head>
<body>
    <center>
  <nav>
    <%= erb(:navbar)%>
  </nav>
    <%= erb(:title) %>
    <%= erb(:signinforms, :locals => { :action => "/signin", :name => "Submit" }) %>
    <br>
    <%= erb(:footer)%>
  </center>
</body>
</html>
