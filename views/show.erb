<h1>List of all the peeps:</h1>
<ul>
  <% @peeps.each do |peep| %>
  <li class='peep' id='peep-<%= peep.id %>'>
    <a><%= peep.peep %>  <%= peep.time.strftime(" at %H:%M") %></a>
  </li>
  <% end%>
</ul>
