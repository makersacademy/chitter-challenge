require 'database_helpers'

require 'comment'
require 'peep'

describe Comment do
  describe '.create' do
    # it 'creates a new comment' do
    #   maker = User.create(email: 'test@example.com', password: 'password123', name: 'testname', username: 'username')
    #   peep = Peep.new(peep: "this is a new peep", maker: maker.id)
    #   comment = Comment.create(comment: "this is a new comment", peep: peep.id, maker: maker.id)

    #   persisted_data = persisted_data(table: 'comments', id: comment.id)
    #   expect(comment).to be_a Comment
    # end
  end
end