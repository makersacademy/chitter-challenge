require './lib/peeps.rb'
describe Peeps do
  it 'saves new peep to the database' do
    # trunc_test_tables
    # new_peep = Peeps.create('my first peep')
    # expect(new_peep.text).to eq 'my first peep'
  end
end
