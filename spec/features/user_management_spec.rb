feature 'User sign up' do

  # xscenario 'can sign up as a new user' do
  #   expect { sign_up }.to change(User, :count).by(1)
  #   expect(page).to have_content('alice@example.com ExemplaryAlice')
  #   expect(User.first.email).to eq('alice@example.com')
  #   expect(User.first.username).to eq('ExemplaryAlice')
  # end

  # def sign_up(username: 'ExemplaryAlice', email: 'alice@example.com', password: 'wonderland')
  #   visit 'users/new'


  # end
end