Welcome back <%= @name %>
  <form method="post" action="/session">
    <input type="hidden" name="_method" value="delete">
    <input type="submit" value="Logout">
  </form>
<br>
Post a peep here
  <form method="post" action="/main">
    <input type="text" name="message" placeholder="What's on your mind...?">
    <input type="submit" value="Peep">
  </form>
<br>
Or for a private peep, select the Maker
  <form method='get' action='/main/private'>
    <select name='makers'>
      <% @all_users.each do |user| %>
        <option value=<%= user.name %>><%= user.name %></option>
      <% end %>
    </select>
    <input type='submit' value='Private Peeps' name='private'>
  </form>
<br>
Past peeps:
<br>
<% if @all_peeps %>
  <% @all_peeps.each do |peep| %>
    <% if peep.personal_message_to == nil %>
      <% user = @all_users.get(peep.user_id) %>
      <%= user.name %> (<%= user.username %>) said: <%= peep.message %> at <%= peep.time.strftime('%r') %>
      <br>
    <% end %>
  <% end %>
<% end %>