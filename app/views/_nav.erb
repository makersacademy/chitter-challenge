  <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <img class="align-self-center chitter-icon" src="/img/chitter_icon.svg" alt="Chitter icon">
  <a class="navbar-brand px-2 main-font-color" href="/">Chitter</a>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">
      <% if !current_user %>
      <li class="nav-item">
        <a class="nav-link btn" href="/sessions/new" id="sign_in">Sign In</a>
      </li>
      <li class="nav-item">
        <a class="nav-link btn" href="/users/new" id="sign_up">Register</a>
      </li>
      <% else %>
      <li class="nav-item new-peep-button">
        <a class="nav-link btn" href="/peeps/new" id="new_peep">New Peep</a>
      </li>
      <% end %>
    </ul>
    <% if current_user %>
    <form action="/sessions" method="post">
      <input type="hidden" name="_method" value="delete">
      <button class="btn sign_out" type="submit" name="sign_out" id="sign_out">Sign Out @<%= @user.user_name %></button>
    </form>
    <% end %>
  </div>


</nav>
