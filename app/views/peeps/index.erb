<h1>Welcome to Chitter</h1>

<h1>Peeps of the day</h1>

  <ul id="peeps">
    <% @peeps.reverse.each do |peep| %>
      <li>
        <%= peep.user.name %> (<%= peep.user.user_name %>) peeped: <%= peep.peeps %>
        <%= peep.time.strftime('%H:%M, %e %B %Y') %>
      </li>
    <% end %>
  </ul>
