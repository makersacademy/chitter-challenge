<h1>Peeps</h1>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.user.username %>
      <%= peep.created_at.strftime("%a, %d %b %Y %H:%M:%S") %>
      <%= peep.content %>
    </li>
  <% end %>
</ul>
