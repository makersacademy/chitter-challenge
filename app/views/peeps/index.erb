<html>

  <h1>
    Peeps:
  </h1>

  <ul id='peeps'>
    <% @peeps.reverse.each do |peep| %>
      <li>
        Title: <%= peep.title %>
        Content: <%= peep.content %>
        Time: <%= peep.created_at %>
      </li>
    <% end %>
  </ul>
</html>
