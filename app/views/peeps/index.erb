<title>Chitter</title>
<div class='container'>
  <p>Peeps</p>
  <ul id='peeps'>
    <% @peeps.each do |peep| %>
    <li>
      <%= peep.message %>
      <%= peep.time %>
    </li>
    <% end %>
  </ul>
</div>
