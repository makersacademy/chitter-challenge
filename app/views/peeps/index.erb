<h1> Peeps </h1>

<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li>
      Peep: <%= peep.text %>
      Timestamp:  <%= peep.timestamp %>
    </li>
  <% end %>
</ul>
<a href="/">Go to Home Page</a>