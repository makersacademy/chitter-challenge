<h1>Peeps</h1>
<form action="/peeps/new">
  <input type="submit" value="New Peep">
</form>
<ul id="peeps">
  <% @peeps.reverse.each do |peep| %>
  <li> <%= "Name:#{peep.user.name}, username: #{peep.user.username}" %> <br />
  <%= peep.time %> <br />
  <%= peep.peep %>
  </li>
  <% end %>
</ul>
