<h1> Peeps! </h1>

<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li>
      Message:    <%= peep.message %>
      Time:       <%= peep.timestamp %>
      Username:       <%= peep.user.username %>
    </li>
  <% end %>
</ul>

