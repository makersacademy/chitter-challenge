<title>Chitter</title>
<div class='container'>
  <p>Peeps</p>
  <ul id='peeps'>
    <% @peeps.each do |peep| %>
    <li>
      <%= peep.message %>
      <br>
      <%= User.get(session[:user_id]).username %>
      <%= peep.time %>
    </li>
    <% end %>
  </ul>

</div>
