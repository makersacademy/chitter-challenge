<h1> Peeps </h1>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.message %> <%= peep.created_at.strftime("%H:%M") %>
    </li>
  <% end %>
</ul>


<form action='/peeps/new' method='get'>
  <input type='submit' value='Add peep'/>
</form>
