<h1> Peeps </h1>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      Title: <%= peep.title %>
      posted_by: <%= peep.posted_by %>
      created_at: <%= peep.created_at %>
      body: <%= peep.body %>
    </li>
  <% end %>
</ul>
