<h1>Peeps</h1>
<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li id="msgs">
      <%=peep.user.name%> (<%= peep.user.username %>) chitted on
       <%=peep.timestamp.strftime('%e-%b, %k:%M') %> <br>
      <%= peep.message %> <br>
      <a href="/reply?username=<%=peep.user.username%>">reply to peep</a>
    </li>
  <% end %>
</ul>