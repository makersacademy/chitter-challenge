<h1>Peeps</h1>
<% @peeps.each do |peep| %>
  <h4>"<%=peep.message%>"</h4>
  <h5>by <%=peep.user.name%>(@<%=peep.user.username%>)</h5>
  <h7>at <%=peep.created_at.strftime("%H:%M %d-%m-%Y")%></h7>
  <div class="tiny_space"></div>
<%end%>
