<!DOCTYPE html>
<html>
  <head>
    <title>Chitter - List of Peeps</title>
  </head>
  <body>
    <ul id='peeps'>
      <% @peeps.reverse_each do |peep| %>
        <li>
          <%= peep.message %>
          From: <%= peep.user.name %>, @<%= peep.user.username %> at <%= peep.time %>
        </li>
      <% end %>
    </ul>
  </body>
</html>
