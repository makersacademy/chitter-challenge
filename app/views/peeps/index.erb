<h1> Peeps </h1>
<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
       <%= peep.message %>
       <%= peep.created_at  %>
      <%= peep.user.user_id %>
    </li>
  <% end %>
</ul>
<form action="/peeps/new" method="get">
  <input type="submit" value="Click to peep">
</form>
