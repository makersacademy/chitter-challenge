<form action='/peeps/new'>
  <input type='submit' value='New peep'>
</form>

<h1> Peeps </h1>

<ul id="peeps">
  <% @peeps.each do |peep| %>
    <%=time_format(peep)%> <%=peep.user.name%> (aka <%=peep.user.user_name%>) posted:
    <li>
      <%=peep.text %>
    </li>
  <% end %>
</ul>

