<h1>Peeps</h1>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.user.username %>
      <%= peep.created_at.strftime("on %a, %d %b %Y at %H:%M:%S ") %>
      <%= peep.content %>
      <a href='/peeps/<%= peep.id %>'>Reply</a>
      <ul id='replies'>
        <% peep.replies.each do |reply| %>
          <li>
            <%= reply.user.username %>
            <%= reply.created_at.strftime("on %a, %d %b %Y at %H:%M:%S ") %>
            <%= reply.content %>
          </li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
