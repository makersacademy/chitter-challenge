<h1>Peeps</h1>
<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li>
      Message:<%=@user.name%> " " <%= @user.username %>" "
      <%=peep.timestamp.strftime('%e-%b, %k:%M') %> <br>
      <%= peep.message %>
    </li>
  <% end %>
</ul>