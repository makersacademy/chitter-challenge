<div class='view-peeps'>
  <h1> Peeps </h1>
  <ul id='peeps'>
    <% @peeps.each do |peep| %>
    <li>
      Title: <%= peep.title %>
      Content: <%= peep.message   %>
    </li>
    <% end %>
  </ul>
</div>
