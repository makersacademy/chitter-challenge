<h1> Chitter </h1>

<% @peeps.each do |peep| %>
  <div class="peep">
    <div class="content">
      <%= peep.peep %>
      <%= peep.user.email %>
    </div>
  </div>
<% end %>

<a href="/users/new">Sign up</a>
