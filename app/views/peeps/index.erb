<h1>Peeps</h1>
<% if current_user %>
  Welcome, <%= current_user.name %><%= current_user.nickname %>
<% end %>
<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.content %>
      <!-- <%= current_user.name if current_user%>
      <%= current_user.nickname if current_user%> -->
    </li>
  <% end %>
</ul>
