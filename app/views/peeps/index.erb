<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <style>
  body {
      text-align: center;
      background-color: #2BCE88
    }
  ul {
    text-align: center;
    list-style-type: none
  }

  </style>
  <body>
    <h2> Peeps </h2>

    <form action="peeps/new">
      <input type="submit" value="Create new peep">
    </form>

    <ul id='peeps'>
      <% @peeps.each do |peep| %>
      <li>
        <strong>Message:</strong> <%= peep.message %>
      </li>
      <li>
        <strong>Posted</strong> by: @<%= peep.user.username %>
      </li>
      <li>
        <strong>On</strong> <%= peep.timestamp.strftime("%A, %d %b %Y at %l:%M %p") %>
      </li></br>
      <% end %>
    </ul>
  </body>
</html>
