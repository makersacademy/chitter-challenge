<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <body>
    <h1> Peeps </h1>

    <form action="peeps/new">
      <input type="submit" value="Create new peep">
    </form>

    <ul id='peeps'>
      <% @peeps.each do |peep| %>
      <li>
        Message: <%= peep.message %>
      </li>
      <li>
        Posted by: @<%= peep.user.username %>
      </li>
      <li>
        On <%= peep.timestamp.strftime("%A, %d %b %Y at %l:%M %p") %>
      </li></br>
      <% end %>
    </ul>
  </body>
</html>
