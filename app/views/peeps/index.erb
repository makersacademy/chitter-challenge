<!DOCTYPE html>

<html>
<body>
	<h1>Peeps</h1>

	<ul id='peeps'>
		<% @peeps.each do |peep| %>
			<li>
				<%= peep.user.firstname %> <%= peep.user.surname %> @<%= peep.user.username %> <%= peep.created_at %>
				<br>
				<%= peep.text %>
			</li>
		<% end %>
	</ul>

</body>
</html>