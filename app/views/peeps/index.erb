<% @peeps.each do |peep| %>
  <div class="panel panel-default">
    <div class="panel-body">
        <img class="user-avatar img-rounded" src="/img/avatar-default-164.png" alt="avatar">
        <span class="username">@<%= User.get(peep.user_id).username %></span><span class="time"><%= peep_distance_of_time(peep.created_at) %></span>
        <p class="peep-text"><%= peep.text %></p>
    </div>
  </div>
<% end %>
