<h1> Peeps </h1>

<p>
    <a href="/peeps/new">Do a new peep</a>
</p>

<ul id="peeps">
  <% @peeps.each do |peep| %>
    <li>
      User: <%= peep.user %>
      Content:   <%= peep.content  %>
      Time: <%= peep.created_at %>
    </li>
  <% end %>
</ul>
