<h1> Peeps </h1>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      User: <%= peep.user.username %>
      Name:   <%= peep.user.name  %>
      Time: <%= peep.created_at.strftime("%H:%M %d/%m/%Y" )%>
      Peep:   <%= peep.peep   %>

    </li>
  <% end %>
</ul>
