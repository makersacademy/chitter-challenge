<h2> Peeps </h2>

<ul id="peeps">

  <% @peeps.each do |peep| %>
    <li class='peep'>
      <%= peep.user.name %> <%= peep.user.username %> <br>
      <%= peep.content %><br>
      <%= peep.created_at.strftime('%c') %>
    </li>
  <% end %>

</ul>
