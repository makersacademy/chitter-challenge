<ul id='peeps'>
  <% Peep.all(:order => [ :date.desc ]).each do |peep| %>
    <li>
      Username: <%= peep.user.username %>
      Date    : <%= peep.date %>
     <%= peep.text   %>
    </li>
  <% end %>
</ul>