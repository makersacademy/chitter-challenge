<!DOCTYPE html>
<html>
<head></head>

<% @messages.each do |message| %>
  <h3><%= message.user.name %> (@<%= message.user.username %>)</h3>
  <%= message.body %>
  <h6><%= message.created_at %></h6>
  <br><br>
<% end %>

<h4>
  <a href="/write_message" id="write_message">Write a Peep</a></h4>
</h4>
