

<h2>My peeps:</h2>

<ul id='peeps'>
  <% @peeps.each do |peep| %>
    <li>
      <h2> <%= peep.content %> </h2>
          <h6>...peeped at <%= peep.created_at.strftime("%H:%M, %d %B '%y") %></h6>
    </li>
  <% end %>
</ul>
