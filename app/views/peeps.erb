<div class='peeps'>
  <h1> Peep Feed </h1></br>
  <ul id='peeps'>
    <% @peeps.each do |peep| %>
    <p>
      <li><strong>Peep:</strong> <%= peep.peep  %> =>
        <strong>Time:</strong> <%= peep.created_at %>
      </li>
      </p>
      <% end %>
    </ul>
  </div>
