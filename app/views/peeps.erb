<div>
	<h1>Peeps</h1>
		<ul id = 'peeps' >
		  <% @peeps.each do |peep| %>
		<li>
		  <p class="peep"><%= peep.content %></p> posted by
		  <%= peep.user.name %>
		  on <%=(peep.created_at.to_s[0..15].sub! "T", " ")%> 
		</li>
		  <% end %>
		</ul>

</div>