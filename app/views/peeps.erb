<html>
  <head>
    <title> What's on your mind?</title>
  </head>

  <body>
    <% if @user %>
    <h3>Welcome, <%= @user.name%>!</h3>
    <% end %>
    <article>
    <ul>
      <%@peeps.each do |peep|%>
      <li><%="@#{@user.username}: #{peep.content} (#{peep.time})"%></li>
      <%end%>
    </ul>
    </article>
  </body>
</html>
