<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="stylesheet.css">
    <title> Chitter </title>
  </head>

  <h1> Latest Peeps </h1>

  <% if @current_user %>
    Welcome <%= @current_user.username %>!
  <% end %>

  <body>
    <ul>
      <% @peeps.reverse_each do |peep_item| %>
        <li>
          @<%= peep_item.username %> <br>
          <%= peep_item.message %> <br>
          Time: <%= peep_item.time %>
        </li>
        <br>
      <% end %>
    </ul>
  </body>
</html>
