<!--<link rel="stylesheet" href="css.css" type="text/css" /> -->

<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
  </head>
  <body>
      <header>
        <h1> Peeps! </h1>
      </header>
      <div>
        <form action='/peeps' method='post'>
          <label for='peep'>
            Peep away:
            <div>
              <textarea name='peep' maxlength='140' placeholder='Peep here...' cols='35' rows='4'></textarea>
            </div>
          </label>
          <input type='submit' value='Post my peep'>
        </form>
      </div>
      <br>
      <div>
        <dl id='peeps'>
          <% @peeps.each do |peep| %>
            <dt><%= peep.name %> @<%= peep.user_name %></dt>
            <dd><%= peep.body %></dd>
            <br>
          <% end %>
        </ul>
      </div>
  </body>
</html>