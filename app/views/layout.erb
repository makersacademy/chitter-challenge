<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="/reset.css">
    <link rel="stylesheet" type="text/css" href="/app.css">
    <title>Chitter</title>
  </head>
  <body>
  <header>


    <div id='session_controls'>
    <% if current_user %>
    <a href='/session/end'><button class='in_out'>Logout</button></a>
    <span id='welcome-message'> Hello, <%=current_user.name%></span>
    <% else %>
    <a href='/users/new'><button class='in_out'>Sign-up</button></a>
    <a href='/session/new'><button class='in_out'>Login</button></a>
    <%= flash[:notice] %>
    <% end %>
    </div>
    <div id='logo'> Chitter </div>
    <div id='peep_controls'>
      <a href='/session/peep'><button class='peep'>Peep</button></a>
    </div>
  </header>

  <section class='errors'>
 <% if flash[:errors] && !flash[:errors].empty? %>
      <% flash[:errors].each do |error| %>
        <div id='error'><%= error %></div>
      <% end %>
    <% end %>
  </section>

    <%= yield %>
  </body>
</html>
