<!DOCTYPE html>

<html>
  <head>
    <title>Chitter</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.username %>!
    <form action="/logout" method="post">
      <input type="submit" value="Log out">
    </form>
    <% end %>
    <%= yield %>
  </body>
</html>
