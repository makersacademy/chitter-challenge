<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
      <form action = "/logout" >
        <input type = "submit" value = "logout">
      </form>
    <% end %>
    <%= yield %> 
  </body>
</html>
