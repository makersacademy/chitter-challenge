<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
  </head>

  <body>

    <%= partial :session %>


    <% if flash[:errors] && !flash[:errors].empty? %>
      There are the following errors:
      <ul id='errors'>
        <% flash[:errors].each do |error| %>
          <li>
            <%= error %>
          </li>
        <% end %>
      </ul>
    <% end %>

    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
