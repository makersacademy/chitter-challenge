<!DOCTYPE html>
<html>

  <head>
    <title>CHITTER</title>
  </head>

  <h1>CHITTER</h1>
  
  <% if flash[:notice] %>
    <div id='notice'><%= flash[:notice] %></div>
  <% end %>

  <body>

    <% if current_user %>
      Welcome, <%= current_user.username %>
    <% end %>
    <%= yield %>

  </body>

</html>
