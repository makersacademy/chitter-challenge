<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset='UTF-8'>
    <!-- <link rel='stylesheet' href='styles.css'/> -->
  </head>
  <body>
    <% if flash[:error_message] %>
      <h2><%= flash[:error_message] %></h2>
    <% end %>
    <% if current_user %>
      <h3>You are logged in as <%= current_user.name %> (@<%= current_user.username %>)</h3>
    <% end %>
    <%= yield %>
  </body>
</html>
