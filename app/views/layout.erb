<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.name %>
    <% end %>
    <%= flash[:email_taken] if flash[:email_taken] %>
    <%= flash[:username_taken] if flash[:username_taken] %>
    <%= flash[:name_missing] if flash[:name_missing] %>
    <%= yield %>
  </body>
</html>
