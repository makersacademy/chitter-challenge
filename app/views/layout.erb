<!DOCTYPE html>
<html>
<link href="https://fonts.googleapis.com/css?family=Crete+Round:400,400i|Raleway" rel="stylesheet">
<link rel="stylesheet" type="text/css" href="mystyle.css">
  <head>
    <title>chitter</title>
  </head>
  <h1>chitter</h1>
</body>
  <% if current_user %>
    Welcome back, <%= current_user.username %>
      <form method='post' action='/sessions'>
        <input type='hidden' name= '_method' value='delete'>
        <input type='submit' value = 'Sign Out'>
      </form>
  <% end %>

  <% if flash[:notice] %>
    <div id='notice'>
    <%= flash[:notice] %>
    </div>
  <% end %>

  <% if flash[:error] %>
    <div id='error'><%= flash[:error] %></div>
  <% end %>

  <%= yield %>
</body>
</html>
