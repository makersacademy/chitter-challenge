<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
        <link rel="stylesheet" type="text/css" href="chitter.css">
  </head>

  <body>
    <style>
      body {font-family:Tahoma;}
      .title {color:rgb(110,191,255); border:solid 1px darkblue; border-radius:5px;
            padding:15px;}
    </style>
    <h1 class="title">
      <a href="http://tinypic.com?ref=2djvxbq" target="_blank"><img src="http://i65.tinypic.com/2djvxbq.jpg" border="0" alt="Image and video hosting by TinyPic" height="70px" ></a>
      Chitter </h1>


    <% if flash[:errors] && !flash[:errors].empty? %>
      <ul id='errors'>
        <% flash[:errors].each do |error| %>
          <li>Sorry. <%= error %></li>
        <% end %>
      </ul>
    <% end %>

    <% if current_user %>
      Welcome to Chitter, <%= current_user.username %>
      <form method='post' action='/sessions'>
        <input type='hidden' name='_method' value='delete'>
        <input type='submit' value='Log out'>
      </form>
    <% end %>

    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>  <!-- this then yields to the view which has been called by your controller -->
  </body>
</html>
