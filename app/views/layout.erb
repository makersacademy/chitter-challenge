<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
      <p></p><center>
        <img src="http://bit.ly/1RI9rlt">
      <p></p>
    </center>
  </head>

  <body bgcolor="#F5F5F5">
    <center>
      <font face="verdana">

        <% if flash[:errors] && !flash[:errors].empty? %>
        Refer to the following errors below:
        <ul id='errors'>
          <% flash[:errors].each do |error| %>
            <li> <%= error %></li>
            <% end %>
          </ul>
        <% end %>
        <%= yield %>

        <% if current_user %>
          <p> </p>
          <form method='post' action='/sessions'>
            <input type='hidden' name='_method' value='delete'>
            <input type='submit' value='Sign out'/>
          </form>
        <% end %>
      </font>
    </center>
  </body>
</html>
