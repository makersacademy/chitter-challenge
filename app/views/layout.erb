<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Chitter</title>
  </head>
  <body>
    <% if current_user %>
      <h2>Signed in as <%= current_user.username %></h2>
    <% end %>
    <%= yield %>
  </body>
</html>
