<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
    <link href="<%= url("/css/bootstrap.min.css")%>" rel="stylesheet">
    <link href="<%= url("/css/ie10-viewport-bug-workaround.css")%>" rel="stylesheet">
    <link href="<%= url("/css/starter-template.css")%>" rel="stylesheet">
  </head>
  <body>
    <div class="container-fluid">
    <div class="row">
    <nav class="navbar navbar-default navbar-static-top" role="navigation">
     <div class="container">
       <div class="navbar-header">
         <a class="navbar-brand" href="/">Chitter</a>
       </div>
       <div id="navbar" class="collapse navbar-collapse">
         <ul class="nav navbar-nav">
           <% unless current_user %>
             <li><a href="/users/signup"><button class="btn btn-default" >Signup</button></a></li>
             <li><a href="/users/login"><button class="btn btn-default" >Login</button></a></li>
           <% end %>
           <% if current_user %>
             <form action="/users/logout" method="post">
               <input name="_method" type="hidden" value="delete">
               <button type="submit" class="btn btn-default navbar-btn">Logout</button>
             </form>
           <% end %>
         </ul>
       </div><!--/.nav-collapse -->
     </div>
   </nav>
   <div class="row">
     <div class="col-md-5 col-md-offset-3 text-center">
      <% if flash[:errors] %>
        <% flash[:errors].each do |error| %>
        <div class="alert alert-danger">
          <%= error %>
        </div>
        <% end %>
      <% end %>
      <% if flash[:notice] %>
        <div class="alert alert-warning">
          <%= flash[:notice] %>
        </div>
      <% end %>
        <%= yield %>
     </div>
   </div>
  </div>
</div>
  <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
