<!DOCTYPE html>
<html>
  <head>
    <title>Chiteroo</title>
  </head>

  <body>
    <% if current_user %>
      Hey <%= current_user.username %>!
    <% end %>
    <%= yield %>  <!-- this then yields to the view which has been called by your controller -->
  </body>
</html>
