<!DOCTYPE html>

<html>
  <head>
    <title> Chitter</title>
  </head>

  <body>
    <% if current_user %>
    Logged in as: <%= current_user.username %>
    <% end %>
    <%= yield %>

    <% if flash[:error] %>
      <div id='error'> <%=flash[:error] %> </div>
    <% end %>

  </body>
</html>
