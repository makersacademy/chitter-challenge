<!DOCTYPE html>
<html>
  <head>
    <title>Chitter | Post and read peeps!</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>
    <%= yield %>  
  </body>
</html>