<!DOCTYPE html>
<html>
  <head>
    <title>Chitter Chitter Yang-Yang</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
      <form method='post' action='/sessions'>
        <input type='hidden' name='_method' value='delete'>
        <input type='submit' value='Sign out'>
      </form>
    <% else %>
      To Peep You Must Be a Peeper! Please Sign In And Be One Step Closer To Being Complete!
      <form method='get' action='/sessions/new'>
        <input type='submit' value='Sign in (existing account)'>
      </form>
      <form method='get' action='/users/new'>
        <input type='submit' value='Sign up (new account)'>
      </form>
    <% end %>
    <% if flash[:notice] %>
      <div id= 'notice'><%= flash[:notice] %></div>
    <% end %>
    <%= yield %>
  </body>
</html>