<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css?family=Comfortaa" rel="stylesheet" type="text/css">
    <style>
    <% c1 = ['aquamarine', 'coral', 'gold', 'crimson', 'dodgerblue', 'BlueViolet ', 'lightgreen', 'mediumvioletred', 'slategrey'].sample  %>
    .peep{
      background-color: <%= c1 %>
    }
    #new_peep{
      border-color: <%= c1 %>;
    }
    #new_peep_bod{
      border-color: <%= c1 %>;
    }
    #peep_button{
      border-color: <%= c1 %>;
      background-color: <%= c1 %>;
    }
    .nav{
        background-color: <%= c1 %>;
        }
        label{
    background-color: <%= c1 %>;
    border-color: <%= c1 %>;
        }
          #login_button{
    border-color: <%= c1 %>;
    background-color: <%= c1 %>;
          }
          #register_button{
    border-color: <%= c1 %>;
    background-color: <%= c1 %>;
          }
          #entry{
                border-color: <%= c1 %>;

          }
      </style>
    <title>Chitter</title>
  </head>
  <body>

  <div class='nav'>
    <div id='current_user'>
    <%= current_user.username if current_user %>
  </div>
    <% if current_user %>
      <div class='sign'>
        <form class="buttons" action="/sessions" method="post">
          <input type="hidden" name="_method" value='delete'>
          <div class='button'><input id='sign_button'type="submit" value='log out'></div>
        </form>
      </div>
      <% else %>
      <div class='sign'>
        <form class="buttons" action="/sessions/new" method="get">
          <div class='button'><input id='sign_button' type="submit" value='sign in'></div>
        </form>
        <form class="buttons" action="/accounts/new" method="get">
          <div class='button'><input id='sign_button' type="submit" value='sign up'></div>
        </form>
      </div>
    <% end %>
  </div>
  <%= partial :errors %>
  <%= partial :notes %>
  <%= yield %>
  </body>
</html>
