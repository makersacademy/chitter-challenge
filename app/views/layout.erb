<head>
  <link rel="stylesheet" type="text/css" href='/css/reset.css'>
  <link rel="stylesheet" type="text/css" href='/css/main.css'>
</head>
<body>
  <header>
    <h1 id='logo'><a href='/'>TheChitter</a></h1>
    <% if @user %>
      <%= "Happy Chitting, #{@user.username}" %>
      <form action='/users/sign-out' method='post'>
        <button>Sign out</button>
      </form>
      <%= "<a href='/users/profiles/#{@user.username}'>My Profile</a>" %>
      <a href='/users/profiles/<%= @user.username %>/following'>Following</a>
    <% else %>
      <form action='/users/new' method='get'>
        <button>Register</button>
      </form>
      <form action='/sessions/new' method='get'>
        <button>Login</button>
      </form>
    <% end %>
  </header>
  <% if flash[:notice]%>
    <div class="flash notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <%= yield %>
</body>
