<!DOCTYPE html>
<html>
  <head>
    <title>Chitter, your daily dose of anger</title>
  </head>

  <body>
  <h1>Chitter, this week in stupid</h1>
    <% if current_user %>
      <div id='welcome' >
      Welcome, <%= current_user.name %>
      </div>

      <form method='post' action='/sessions'>
        <input type='hidden' name='_method' value='delete'>
        <input type='submit' value='Log out'>
      </form>

      <div id='new_peep'>
        <form method='post' action='/peeps'>
          <label for='content'>
            Rage here: <input name='content' type='text' placeholder='Remember, nobody cares'required>
          </label>
          <input type='submit' value='peep'>
        </form>
      </div>

    <% end %>

    <% if flash[:errors] %>
      <div id='errors'>
        <% flash[:errors].each do |error| %>
          <p> <%= error %> </p>]
        <% end %>
      </div>
    <% end %>

    <% if flash[:notice] %>
      <div id='notice'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>  <!-- this then yields to the view which has been called by your controller -->
  </body>
</html>
