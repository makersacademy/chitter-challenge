<!DOCTYPE html>
<html>
  <head>
    <title>Chitter, your favorite chit chat room!</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>
    <%= yield %>  
  </body>
</html>
