<!DOCTYPE html>
<html>
  <head>
    <title>Chitter</title>
  </head>
  <body>
  
  <% Peep.all(order: [ :id.desc ]).each do |peep| %>
  <p> <%= peep.name %> @<%= peep.username %> <br>
  	  <%= peep.message %></p>
  <% end %>

  </body>
</html>