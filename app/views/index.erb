<h1>Peeps</h1>

<ul id='peep'>

  <% @peep.each do |peep| %>
    <li>
      <!-- Title: <%= link.bookmark_name %> -->
      Username:  <%= peep.user.map{|user| user.username}..join('') %>
      Peep: <%= peep.peep %>
    </li>
  <%end%>
  </ul>
