<h1>Welcome to Chatter</h1>

Current Peeps:
<ul>
  <% Message.all(order:[:time.desc]).each do |message| %>
    <li><a ><%= message.peep %> </a></li>
    (Peeped by "<%= message.user.name %>" - <%= message.time%>)
  <% end %>
</ul>

<div id='new-peep'>
  <form action='/peeps' method='post'>
    Peep: <input type='text' name='peep'>
    <input type='submit' value='Add Peep'>
  </form>
</div>
<!-- This should probably be within sign in page not on homepage -->