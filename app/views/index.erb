<div id='peeps-container'>
  <div id="new-peep">
    <ul>
    Peeps
    <% Peep.all(order: [:time.desc]).each do |peep| %>
      <li>
        <%= peep.handle %>
        Content:   <%= peep.message %>
        Time:      <%= peep.time %>
      </li>
    <% end %>
    </ul>
  </div>
</div>
