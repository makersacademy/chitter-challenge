<h2>Chitter Peeps</h2>

<ul>
  <% @peeps.reverse_each { |peep| %>
  <li><%= "#{peep.name} says, \"#{peep.content}\""%>
  <h6><%= "#{peep.time_submitted.strftime("%a, %b, %y at %I:%M%p") }" %></h6>
  </li><% }%>
</ul>