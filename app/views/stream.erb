<section class='stream'>
		
	    <%@global_stream.each do |mess|%>
	<p class='peep'>
		
	 <%= p @users.all(id: mess.user_id) %> said: </br> <%= mess.body %> </br>
	 on: <%= mess.date.strftime( "%d/%m/%Y at %I:%M%p" )%> </br>
    </p>
	
	  <% end %>
	  
</section>