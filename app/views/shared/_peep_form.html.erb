<%= form_with(model: @peep, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Make a new peep..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>