<h1>Latest peeps <i>(totally not tweets)</i></h1>

<% @peeps.each do |peep| %>
	<div class="peep">
		<img src="https://robohash.org/<%= User.get(peep.user_id).username %>?set=set2">
		<h4><%= User.get(peep.user_id).username %></h4>
		<p>
			<%= peep.text %>
		</p>
		<i>Peeped at <%= peep.created_at.strftime("%A, %b %d %r")%></i>
	</div>
<% end %>

<style type="text/css">

</style>