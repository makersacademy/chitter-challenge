default: &default
  adapter: postgresql
  encoding: unicode

development:
  <<: *default
  host: YOUR_DATABASE_URL
  username: DB_USERNAME
  password: DB_PASSWORD
  database: chitter

test:
  <<: *default
  host: localhost
  database: chitter_test
