default: &default
  adapter: postgresql
  encoding: unicode

development:
  <<: *default
  host: YOUR_DATABASE_URL
  database: chitter_development

test:
  <<: *default
  host: localhost
  database: chitter_test
